# PR Description Template

Every pull request (or merge request) produced by a bolt uses this 8-section structure.
The orchestrator populates all sections. The human reviews and approves.

---

## Summary

[What was built and why — one paragraph. What problem does this solve and for whom?]

## Ticket

[Ticket ID and full URL from topology.yaml → ticket_url_pattern]

## Blast Radius Report

- **Affected systems:** [list]
- **Score:** Low | Medium | High | Critical
- **Cross-system impact verified:** Yes | No — [explanation if No]
- **ARB convened:** Yes (see decision record at .claude/memory/decisions/) | No

## Changes

- [Key change 1 — be specific: file, function, behavior]
- [Key change 2]
- [Key change 3]
- [Migration: up and down — reference migration file path]

## Test Coverage

- **New tests:** [count and description]
- **Coverage delta:** [before% → after%]
- **Test types included:** unit | integration | contract | E2E | performance
- **Edge cases covered:** [list any non-obvious cases tested]

## Compliance Checklist

- [ ] No secrets, credentials, or API keys in code or logs
- [ ] PII handling reviewed and documented (if `pii_involved: true`)
- [ ] Audit log entries written for all significant actions (if `soc_relevant: true`)
- [ ] Auth boundary changes reviewed by InfoSec (if `auth_boundary: true`)
- [ ] Down migration exists and tested (if schema changed)
- [ ] No new dependencies introduced without architecture justification
- [ ] Rollback tested or rollback plan verified feasible

## Rollback Plan

[How to revert this change if it causes a production incident.]

"Revert commit X" is acceptable only for Low blast radius changes with no schema modifications.

For anything else, describe:
1. The revert action (git revert, feature flag flip, deploy previous tag)
2. Estimated time to revert
3. Any data migration required to revert
4. Downstream systems that must be notified

## Human Review Notes

[What the reviewer should pay particular attention to. What tradeoffs were made. What the ARB recommended. What the agent is uncertain about. What requires domain expertise to validate.]

This section is written by the orchestrator but should be updated by the human reviewer before approval.

---

*Generated by agentic-sdlc · Bolt timestamp: [ISO8601] · Orchestrator: claude-opus*
